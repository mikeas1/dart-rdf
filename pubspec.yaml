name: rdf
description: A library for working with RDF data written in native Dart.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  build: ^2.2.1
  built_collection: ^5.1.1
  built_value: ^8.1.4
  http: ^0.13.4
  petitparser: ^4.4.0
  quiver: ^3.0.1+1
  source_gen: ^1.2.1

dev_dependencies:
  build_runner: ^2.1.7
  built_value_generator: ^8.1.4
  lints: ^1.0.0
  test: ^1.16.0
